using UnityEngine;

namespace _ProjectBoy.Scripts.Audio
{
    public class SoundGamplay : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}